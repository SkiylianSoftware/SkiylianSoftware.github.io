# Bundler cache
.bundle
vendor

# Jekyll cache
.jekyll-cache
.jekyll-metadata
_site

# RubyGems
*.gem

# NPM dependencies
node_modules
package-lock.json

# IDE configurations
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json

# Misc
_sass/vendors
assets/js/dist

# Python virtual env
.venv
__pycache__
*.pyc

# Environment variables (local secrets)
.env

# Data (generated by CI)
_data/*.json
# PC parts are semi-static - keep tracked as CI fallback
!_data/pc_parts.json
# Playlist covers (downloaded per CI run)
assets/img/playlists/
# Branded OG share cards (generated per CI run)
assets/img/og/
# Posts (generated for RSS feed)
_posts/
# Archive pages (generated for series/game landing pages)
archive/